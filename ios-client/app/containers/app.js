var React = require('react-native');
var BudgetalApp = require('./BudgetalApp');

var {
  Component,
  View
} = React;

var App = React.createClass({
  render() {
    return (

      <BudgetalApp />

    );
  }
});

module.exports = App;
