defaults: &defaults
  adapter: postgresql
  pool:     <%= ENV.fetch('RAILS_DATABASE_POOL', 5) %>
  timeout:  <%= ENV.fetch('RAILS_DATABASE_TIMEOUT', 5000) %>
  host:     <%= ENV.fetch('RAILS_DATABASE_HOST', 'localhost') %>
  database: <%= ENV.fetch('RAILS_DATABASE') %>
  user:     <%= ENV['RAILS_DATABASE_USER'] %>
  password: <%= ENV['RAILS_DATABASE_PASSWD'] %>

development:
  <<: *defaults

test:
  <<: *defaults
  database: <%= ENV.fetch('RAILS_TEST_DATABASE', 'budgetal_test') %>

staging:
  <<: *defaults

beta:
  <<: *defaults

production:
  <<: *defaults

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
