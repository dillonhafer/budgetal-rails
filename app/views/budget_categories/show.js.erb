$(".category-ajax").html("<%= escape_javascript(render(partial: 'show', locals: {c: @budget_category})) %>");
$('.category-ajax').foundation();
BudgetCategory.initDragAndDrop()
$('.reveal-modal').foundation('reveal','close');
